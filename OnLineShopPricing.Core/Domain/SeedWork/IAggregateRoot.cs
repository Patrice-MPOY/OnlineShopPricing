namespace OnlineShopPricing.Core.Domain.SeedWork
{
    /// <summary>
    /// Marker interface to identify aggregate roots in the domain model.
    /// 
    /// This empty interface serves only as a semantic tag. It helps:
    /// - Clearly distinguish aggregate roots from other domain objects
    /// - Enforce DDD conventions through code structure and tooling
    /// - Facilitate future refactoring, scanning, or validation (e.g., architecture tests, serialization rules)
    /// </summary>
    public interface IAggregateRoot
    {
        // Intentionally left empty – acts purely as a marker / tag
    }
}
