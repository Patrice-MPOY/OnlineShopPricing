namespace OnlineShopPricing.Core.Domain.Events;

/// <summary>
/// Marker interface for domain events.
/// In a full application, this would be used by the infrastructure
/// to publish events (MediatR, message bus, outbox, etc.).
/// </summary>
public interface IDomainEvent
{
}